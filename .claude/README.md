# Shared Claude Code artifacts

Skills (`.claude/skills/<name>/SKILL.md`) and subagents (`.claude/agents/*.md`)
placed here are committed to the workspace remote and shared with everyone who
clones it. Put team-wide, reusable things here. Keep personal-only skills in your
user-level `~/.claude/` instead.
